# 前端 Claude 智能 Agent 实施方案 (agent.md)

**版本**: 1.0
**状态**: 定稿

## 1. 概述

本文档旨在为在现有的 React/UmiJS 项目中，集成一个由 Claude 模型驱动、能够深度理解并自主操作应用界面的智能 Agent，提供一份全面的技术设计与实施方案。

---

## 2. 核心架构：Agent Core Controller

我们将构建一个名为 **`Agent Core Controller`** 的核心模块，作为 Agent 的大脑和中枢神经系统。它将以“工具调用” (Tool-Using) 模式为基础，负责驱动整个交互流程。

### 2.1. 工作职责

-   **监听用户输入**: 捕获用户通过特定 UI（如聊天框）下达的自然语言指令。
-   **构建上下文**: 调用 **UI 上下文模块**，生成当前界面的结构化“摘要”。
-   **发现可用工具**: 动态地从 **工具注册中心** 获取在当前页面和上下文中可用的工具列表。
-   **驱动语言模型**: 将用户目标、UI 摘要、可用工具和对话历史组装成一个结构化的 **Prompt**，并发送给 Claude API。
-   **执行指令**: 接收并解析 Claude 返回的工具调用指令（一个 JSON 对象），并命令 **工具执行器** 执行。
-   **管理状态**: 维护对话历史，包括用户的指令、Agent 的思考过程（工具调用）以及工具的执行结果。

### 2.2. 工作流程：目标驱动循环

Agent 的工作模式是一个持续的“观察-思考-行动”循环，直到完成用户的最终目标。

1.  **观察 (Observe)**: 生成当前 UI 的摘要。
2.  **思考 (Think)**: 将目标、观察结果和可用工具发送给 Claude，请求下一步行动。
3.  **行动 (Act)**: 执行 Claude 返回的工具调用指令，改变应用状态。
4.  **重复**: 回到步骤 1，直到任务完成。

---

## 3. 关键模块实现

### 3.1. UI 上下文表示 (UI Context)

为避免向模型发送冗余的 DOM 信息，我们将生成一个简化的 **UI 摘要**。

-   **实现方式**: 优先考虑利用浏览器的 **Accessibility Tree**。它本身就是对 UI 语义化和可交互性的高度概括，比手动遍历 DOM 更高效、更准确。
-   **数据结构**: 输出为一个 JSON 对象，清晰地列出所有可交互元素的 `id` (或 `data-testid`)、`type`、`label` 或 `text` 等关键信息。

### 3.2. 工具管理 (Tool Management)

-   **原子化工具 (Atomic Tools)**: 工具应保持原子性，即一个工具只做一件明确的事（如 `clickElement`, `fillInput`）。复杂的业务逻辑（如“提交表单”）应由 Claude 通过组合调用原子工具来完成。
-   **工具注册中心 (Tool Registry)**: 一个全局模块，用于定义和存储所有可用的工具。每个工具都应包含 `name`, `description`, 和 `parameters` (JSON Schema)。
-   **动态发现 (Tool Discovery)**: Agent Core Controller 应与路由系统集成。在不同页面或不同应用状态下，只向 Claude 提供当前上下文中最相关的工具，以减少干扰，提升决策质量。
-   **业务逻辑封装**: 可以将项目中 `src/services` 下的核心业务函数（如 `fetchBills()`）封装成 Agent 可用的工具，让 Agent 具备直接调用后端服务的能力。

### 3.3. Prompt 设计

这是连接前端与 Claude 大脑的桥梁，其结构至关重要。我们将采用包含多个部分的结构化 Prompt：

```
# 角色
你是一个内嵌于网页应用中的 AI 助手，通过调用工具来帮助用户完成任务。

# UI 上下文
这是当前页面的简化摘要，你可以使用元素的 id 来操作它们。
{...} // UI 摘要 JSON

# 可用工具
你只能使用以下工具，并以 JSON 格式响应你的调用请求。
[...] // 可用工具的 JSON Schema 定义

# 对话历史与工具结果
// 此处包含用户与 AI 的交谈记录，以及每次工具调用的结果
User: "帮我查一下账单。"
AI (tool-use): { "tool": "getBillsList", "args": {} }
Tool Result (tool-result): { "tool_name": "getBillsList", "status": "Success", "content": [...] }

# 用户最新指令
User: "下载八月份的账单。"
```

### 3.4. 工具结果反馈 (Tool Result Feedback)

工具执行后，必须将结果结构化地反馈给模型，这是 Agent 进行后续决策的关键依据。

-   **格式**: 反馈信息应包含 `tool_name`, `status` (`Success` 或 `Error`)，以及 `content` (成功时返回的数据或失败时的错误信息)。
-   **作用**: 
    -   **数据驱动**: 让模型可以看到工具调用的返回数据，并利用这些数据进行下一步操作。
    -   **错误处理**: 让模型知道操作失败，并可以决定是重试、换一种方法，还是向用户报告问题。

---

## 4. 实施路线图

1.  **(第一阶段) 基础建设**: 
    -   在 `src/` 下创建 `agent` 目录。
    -   搭建 `Agent Core Controller` 的基本骨架。
    -   实现基于 **Accessibility Tree** 的 UI 摘要生成器。
    -   建立工具注册中心，并定义 3-5 个最基础的原子工具 (`navigateTo`, `clickElement`, `fillInput`)。
    -   在项目中推行 `data-testid` 的规范化使用。

2.  **(第二阶段) 打通循环**: 
    -   实现完整的 Prompt 构建逻辑。
    -   集成 Claude API，完成一次完整的“观察-思考-行动”循环。
    -   从一个简单的、单页面的功能开始试点（如“搜索功能自动化”），验证端到端链路。

3.  **(第三阶段) 能力扩展**: 
    -   丰富工具库，封装更多业务逻辑。
    -   优化工具的动态发现机制。
    -   完善错误处理和用户反馈机制。
    -   探索更复杂的任务场景，如跨页面流程自动化。
